name: islandaidhub_app
description: Island Aidhub - Insigida nga bulig para sa mga Ka-Isla!
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  flutter_dotenv: ^5.0.2
  firebase_core: ^2.7.0
  cloud_firestore: ^4.7.0
 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
